{% block body %}
    <nav>
        {% if is_granted('ROLE_USER') %}
            <a href="{{ path('logout') }}">Logout</a>
        {% endif %}
    </nav>

    {% block content %}{% endblock %}
{% endblock %}
